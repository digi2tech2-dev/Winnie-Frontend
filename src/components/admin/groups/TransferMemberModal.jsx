import { createPortal } from "react-dom";
import { ArrowLeftRight, X } from "lucide-react";
import { useState } from "react";

export default function TransferMemberModal({member,groups,onClose,onTransfer}){if(!member)return null;return createPortal(<Content member={member} groups={groups} onClose={onClose} onTransfer={onTransfer}/>,document.body)}
function Content({member,groups,onClose,onTransfer}){const [target,setTarget]=useState("");return <div className="fixed inset-0 z-[150] grid place-items-center bg-slate-950/65 p-4"><section className="w-full max-w-[420px] rounded-[26px] bg-white p-4 shadow-2xl dark:bg-[#111827]"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-sky-500/10 text-sky-600"><ArrowLeftRight className="h-5 w-5"/></span><div className="min-w-0 flex-1"><h2 className="text-sm font-black text-slate-950 dark:text-white">نقل عضو</h2><p className="truncate text-[9px] font-bold text-slate-400">{member.name}</p></div><button type="button" onClick={onClose}><X className="h-4 w-4"/></button></div><label className="mt-4 block"><span className="mb-1 block text-[9px] font-black text-slate-500">المجموعة الجديدة</span><select value={target} onChange={e=>setTarget(e.target.value)} className="h-11 w-full rounded-2xl border border-slate-200 bg-slate-50 px-3 text-xs font-black dark:border-white/10 dark:bg-[#0B1220] dark:text-white"><option value="">اختر مجموعة أخرى</option>{groups.filter(g=>g.id!==member.groupId).map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select></label><div className="mt-4 grid grid-cols-2 gap-2"><button type="button" onClick={onClose} className="h-10 rounded-xl border border-slate-200 text-[10px] font-black dark:border-white/10 dark:text-white">إلغاء</button><button type="button" disabled={!target} onClick={()=>onTransfer(target)} className="h-10 rounded-xl bg-sky-600 text-[10px] font-black text-white disabled:opacity-40">نقل العضو</button></div></section></div>}
